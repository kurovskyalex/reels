{
  "name": "video-survey",
  "version": "1.0.0",
  "description": "Video quality survey with Google OAuth",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test.js"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
